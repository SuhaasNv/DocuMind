# Required for local dev. Vite only exposes variables prefixed with VITE_.
# Copy to .env and set your backend URL. Restart the Vite dev server after changing .env.
#
# Backend API base URL (no trailing slash). All frontend API calls use this.
VITE_API_URL=http://localhost:3000
